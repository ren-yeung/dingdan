{
  "name": "yijia-erp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "翼嘉通讯-天耘科技 SD-WAN 专线 ERP（Cloudflare 原生版：Pages + Functions + D1 + R2）",
  "scripts": {
    "dev": "wrangler pages dev frontend/dist",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "deploy": "wrangler pages deploy frontend/dist"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
